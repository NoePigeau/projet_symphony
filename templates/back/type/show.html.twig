{% extends 'base_back.html.twig' %}

{% block title %}Type {{ type.name }}{% endblock %}

{% block body %}
    <div class="page-list">
        <div class="card-profile">  
                <h1 class="text-4xl"> Information </h1>
                <div> <strong>Name</strong> : {{ type.name }} </div>
                <div> <strong>Created At</strong> : {{ type.createdAt|date("d/m/Y")}} </div>
        </div>
        <a href="{{ path('admin_type_index') }}" >  back to types list</a>
    </div>
{% endblock %}